# Copy to .env for Docker Compose. Set private values before starting.
JWT_SECRET=
DEMO_PASSWORD=
DEMO_EMPLOYEE_ID=E0002
# AI owner supplies these separately. Never send them to the browser.
OPENAI_API_KEY=
OPENAI_MODEL=
